export interface items {
  id: number,
  name: string,
  price: number,
  createdAt: string,
  store_id: number
}

export interface Createitems {
  name: string,
  price: number,
  store_id: number
}
